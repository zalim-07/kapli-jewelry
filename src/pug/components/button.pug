mixin uiButton(options = {})
  - const tag = options.href ? 'a' : 'button'
  - const type = options.type || 'button'
  - const variant = options.variant || 'black'
  - const disabled = options.disabled || false
  - const className = ['ui-button', `ui-button--${variant}`, options.fullWidth ? 'ui-button--full' : null, options.class || null].filter(Boolean).join(' ')

  if tag === 'a'
    a(
      class=className
      href=disabled ? null : options.href
      aria-disabled=disabled ? 'true' : null
      tabindex=disabled ? -1 : null
    )&attributes(options.attrs || {})= options.label || options.text
  else
    button(
      class=className
      type=type
      disabled=disabled ? true : null
    )&attributes(options.attrs || {})= options.label || options.text
